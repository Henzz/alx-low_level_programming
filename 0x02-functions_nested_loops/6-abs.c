#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: parameter to check
 *
 * Return: 1 if c is lowercaser.
 * On error, 0.
 */
int _abs(int c)
{
	return (abs(c));
}
