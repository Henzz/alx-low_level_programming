/* #!/bin/bash */
#include <stdio.h>
#include <unistd.h>

/**
 * main - writes the character _putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	write(1, "_putchar\n", 10);

	return (0);
}
