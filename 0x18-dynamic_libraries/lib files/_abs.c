#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: parameter to check
 *
 * Return: 1 if n is lowercaser.
 * On error, 0.
 */
int _abs(int n)
{
	return (abs(n));
}
