#include <stdio.h>

/**
 * mul - multiplies to integers
 * @a: integer
 * @b: integer
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
